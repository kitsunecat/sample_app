<% unless current_user?(@user) %>
  <div id="follow_form">
    <%# Jquerryでこの部分だけfollow/unfollow切り替えられるようになっている %>
  <% if current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>
